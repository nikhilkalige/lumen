Your task is to build an android app in kotlin and compose. You are an expert android developer. So make sure that you follow best practices and architechture. No need to create multiple files you can put the seperate classes in the same file>

The main features that are necessary.
1. A settings page to set value of google sheets id
2. On the main page, list of entries, each entry has date, gallons, miles, cost. Also a button to delete, a link to open the google sheets entry on success or display the error that caused it. A button to re add the data to the google sheet if there was an error.
3. The data for the entry is provided using an intent url.
4. Ensure that you handle authentication with google sheets api correctly and in a modern fashion using CredentialManager and AuthorizationRequest.
5. Use a simple store like DataStore to save settings.
6. Properly handle the api tokens, refereshing, saving etc. 


